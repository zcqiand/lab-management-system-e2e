# REQ-2026-101 合同管理 CRUD E2E（铺开第 ① 站）

| 项 | 值 |
|---|---|
| 提出人 | 项目所有者 |
| 提出日期 | 2026-09-13 |
| 优先级 | P0 |
| 状态 | 已验收 |
| 关联 ADR | ADR-0030（锚点断言模式）；ADR-0033 阶段三 |

## 1. 需求描述

**我的理解**：铺开到合同管理（列表/创建/编辑/删除 × 三端），断言锚 BASE 合同管理
功能子项的 data-fn 与可观察行为（行出现/消失；跨仓 ID 字面不入本文档，见 spec 与
function-tree）。写操作自产自销（uniqueCode），用例互不依赖执行顺序。

### 澄清记录

| 疑问 | 结论 | 澄清人 | 日期 |
|---|---|---|---|
| 删除确认交互三端形态不同：react ConfirmModal（纯 button「确认」）/ vue ConfirmDialog（confirm-dialog-confirm 锚）/ nextjs 原生 window.confirm | superset 选择器 + 原生 dialog 自动 accept（helper 双路，已登记分歧），不做 per-端逻辑分叉 | 项目所有者（ADR-0030 Decision 4 精神） | 2026-09-13 |

## 2. 验收标准

| 编号 | 场景 | 操作 | 预期 |
|---|---|---|---|
| AC-1 | 已登录 | 打开 /contracts | seed 行渲染（行锚 data-fn 的数据行 > 0） |
| AC-2 | 列表页 | 新建（六必填）→ 保存 | 行出现 |
| AC-3 | 已建行 | 编辑改项目名称 → 保存 | 行文本更新 |
| AC-4 | 已建行 | 删除 → 确认 | 行消失 |

## 3. 功能影响

| 功能 ID | 名称 | 影响类型 | 说明 |
|---|---|---|---|
| M95.F02.I02 | 合同管理 CRUD 用例 | 新增 | 4 AC × 3 端；覆盖 BASE 合同管理子项（字面见 spec，跨仓 ID 不入本文档） |

## 4. 风险与回滚

删 spec 文件即回滚。
